---
# Kubernetes ServiceAccount with IRSA annotation
apiVersion: v1
kind: ServiceAccount
metadata:
  name: langgraph-agent-sa
  namespace: ai-workloads
  annotations:
    eks.amazonaws.com/role-arn: arn:aws:iam::123456789:role/langgraph-agent-role
